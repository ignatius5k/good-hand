{
  "rules": {
    "sharedGames": {
      "$id": {
        ".read": true,
        ".write": "newData.exists() && newData.hasChildren(['k', 'updated', 'game']) && (!data.exists() || newData.child('k').val() === data.child('k').val())"
      }
    }
  }
}
